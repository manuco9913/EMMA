2026-04-28: Implemented interactive MapLibre GL map with PMTiles offline tile support.
- Ran npm install (all deps now present: maplibre-gl, pmtiles, deck.gl, react-hook-form, zustand, ajv, msw, @types/react, etc.)
- Created src/Map.tsx: registers pmtiles:// protocol, mounts MapLibre map in React via useEffect/useRef, centered on [0, 51.5] zoom 5
- Updated src/App.tsx to render MapComponent full-screen
- Added maplibre-gl CSS import to main.tsx
- Added src/vite-env.d.ts for Vite module type declarations
- PRD items now passing: "Map functions with no internet connection" + "Application opens with an interactive map centered on a default location"
- tsc --noEmit: clean; vitest: 6/6 pass

Next: Implement left-sidebar layout ("Scenario panel is displayed in a left sidebar alongside the map")

2026-04-29: Implemented left-sidebar layout with ScenarioPanel component.
- Created src/ScenarioPanel.tsx: fixed-width (320px) aside element, full-height, scrollable, with grey background
- Updated src/App.tsx: flexbox row layout — ScenarioPanel on left, MapComponent fills remaining width (flex:1 with minWidth:0)
- PRD item now passing: "Scenario panel is displayed in a left sidebar alongside the map"
- tsc --noEmit: clean; vitest: 6/6 pass

Next: Implement scenario form fields rendered from JSON Schema ("Scenario form fields are rendered from the JSON Schema served by the backend at runtime")

2026-04-29: Implemented schema-driven scenario form (SchemaFormRenderer + evaluateShowIf).
- Created src/evaluateShowIf.ts: pure function, accepts ShowIfCondition + values Record, returns boolean; supports all 6 ops (eq/neq/gt/gte/lt/lte), and/or chaining, dot-notation nested paths, missing-field guard
- Created src/evaluateShowIf.test.ts: 17 unit tests, all pass in node environment with no DOM/React/form imports
- Created src/useSchema.ts: React hook that fetches a JSON Schema from a given endpoint; exports JSONSchema type
- Created src/SchemaFormRenderer.tsx: renders form fields from JSONSchema.properties using react-hook-form Controller; supports string/number/boolean/enum/x-ui-component:range; unit suffixes (x-unit); x-show-if conditions via evaluateShowIf; array fields (entities) are skipped for separate feature
- Updated src/ScenarioPanel.tsx: fetches scenario schema from /api/schema/scenario; renders SchemaFormRenderer; no hardcoded field names
- Updated vite.config.ts: added /api → http://localhost:8000 proxy
- PRD items now passing: "Scenario form fields are rendered from the JSON Schema served by the backend at runtime" + "evaluateShowIf is a pure function with no side effects, unit testable without DOM or form context"
- tsc --noEmit: clean; vitest: 23/23 pass

Next: Implement entity management ("User can add between 2 and 10 entities to a scenario") — entity accordion/list inside ScenarioPanel, each entity rendered via SchemaFormRenderer with entity schema from /api/schema/entity

2026-04-29: Implemented entity management (add/remove accordion list) and extended SchemaFormRenderer for entity field types.
- Created src/EntityList.tsx: uses useFieldArray (react-hook-form) with name 'entities'; fetches entity schema from /api/schema/entity via useSchema; renders collapsible accordion per entity; "Add Entity" button disabled at 10; Remove button per entity; warning shown when entity count is 1 (minimum 2 required to run)
- Extended src/SchemaFormRenderer.tsx: added CoordinateField component (renders lat/lon pair side-by-side from object schema with x-ui-component: "coordinate"); added NumericOrFileField component (toggle between number input and file-path text input for x-ui-component: "numeric-or-file"); both check uiComponent before type-based checks in renderField
- Updated src/ScenarioPanel.tsx: renders EntityList inside the scenario form, passing control and watch from the parent useForm
- PRD items now passing: "User can add between 2 and 10 entities", "Entity form fields rendered from entity JSON Schema", "Numeric fields display a unit suffix", "numeric-or-file fields allow toggling"
- Also marked SliceParser PRD item as passing (6 unit tests were already green, PRD just hadn't been updated)
- tsc --noEmit: clean; vitest: 23/23 pass

Next: Implement entity map placement ("User can place an entity by clicking on the map") — onClick handler on MapLibre map emits lat/lon to the active entity's position fields; needs Zustand store or callback to bridge Map and EntityList

2026-04-29: Implemented entity map placement with draggable markers and bidirectional lat/lon sync.
- Created src/mapStore.ts: Zustand store bridging Map ↔ EntityList; holds activeEntityIndex, entityPositions array, and applyPositionToForm callback
- Updated src/Map.tsx: map click handler (reads activeEntityIndex from store via ref, calls applyPositionToForm on click); draggable maplibregl.Marker instances per entity; markers created on first position, updated via setLngLat on position change (EPSILON guard prevents unnecessary updates); isDragging flag prevents form-triggered setLngLat from interfering with active drags; marker click stopPropagation prevents click from also triggering the map click handler; stable ref pattern (applyPositionRef, activeEntityIndexRef) avoids stale closures in event handlers
- Updated src/EntityList.tsx: accepts setValue prop; registers applyPositionToForm with store (calls setValue('entities.N.position', {lat,lon})); watches entities via useWatch and syncs positions array to store on every change; active entity (blue border + "← click map to place" hint) is the last-opened accordion item; opening sets active, closing clears it; remove() correctly adjusts activeEntityIndex when lower-indexed entity is deleted
- Updated src/ScenarioPanel.tsx: destructures setValue from useForm and passes to EntityList
- PRD items now passing: "User can place an entity by clicking on the map", "User can drag an entity marker to update its position", "Lat/lon coordinate fields stay in bidirectional sync with the map marker"
- tsc --noEmit: clean; vitest: 23/23 pass

Next: Implement radius circles ("Each entity's radius is visualized as a filled circle on the map") — GeoJSON fill layer on the MapLibre map, centered on entity position, scaled to radius field value (km), updated in real time as radius changes

2026-04-29: Implemented radius circles as GeoJSON fill layers on the map.
- Created src/circleGeoJSON.ts: pure fn, destination-point formula, returns closed GeoJSON Polygon Feature; 64 segments default
- Created src/circleGeoJSON.test.ts: 6 unit tests (valid GeoJSON shape, closed ring, segment count, all vertices ~radiusKm from center, equator case, default segments); all green
- Extended mapStore.ts: added entityRadii:(number|null)[] + setEntityRadii
- Updated EntityList.tsx: watches entities.*.radius, syncs to store alongside positions
- Updated Map.tsx: added mapReady state (triggered by map 'load' event); circle sync effect adds/updates/removes GeoJSON sources + fill layers per entity; uses setData for updates, removes layer+source when entity removed or radius cleared
- PRD items now passing: "Each entity's radius is visualized as a filled circle on the map" + "Radius circle updates in real time as the radius field value changes"
- tsc --noEmit: clean; vitest: 29/29 pass

Next: TBD

2026-04-29: Verified bootstrap feature (all steps already in place).
- frontend/src/, public/tiles/, contracts/ all exist; tiles.pmtiles present
- npm install exits 0; all required pkgs present (maplibre-gl, deck.gl, react-hook-form, zustand, ajv, msw)
- tsc --noEmit: clean; vitest: 29/29 pass
- Vite dev server confirmed running at localhost:5173
- FastAPI backend confirmed running; GET /api/schema/scenario returns 200 with JSON schema
- PRD item now passing: "Bootstrap: project is scaffolded, dependencies installed, dev server and backend are running, and all required static assets are in place"

2026-04-30: Verified JSON Schema contract files as single source of truth.
- contracts/entity.schema.json and contracts/scenario.schema.json already committed with draft-07 + x- extension properties
- backend/main.py serves both schemas from contracts/ dir (no duplication)
- frontend reads schemas at runtime via useSchema hook (no duplication)
- Created src/contracts.test.ts: 10 tests proving ajv 8 compiles both schemas, resolves $ref from scenario→entity, validates correct objects, rejects invalid ones
- tsc --noEmit: clean; vitest: 39/39 pass
- PRD item now passing: "JSON Schema contract files committed to repo as single source of truth"

2026-04-30: Implemented JSON Schema contract verification (ajv 8 + C# NJsonSchema skeleton).
- Created frontend/src/contracts.test.ts: 10 tests; loads entity.schema.json + scenario.schema.json from /contracts/ via Node fs; uses ajv 8 (strict:false for x- extension keywords); tests draft-07 $schema, x- extension presence, compile-without-throw, $ref resolution, valid entity passes, missing-field entity fails, valid scenario (2 entities) passes, single-entity scenario fails minItems:2
- Created backend-csharp/WaveSim.Contracts.Tests/WaveSim.Contracts.Tests.csproj: xunit + NJsonSchema 11.1.0, net8.0
- Created backend-csharp/WaveSim.Contracts.Tests/SchemaResolutionTests.cs: 6 tests loading schemas from /contracts/ via NJsonSchema.JsonSchema.FromFileAsync; validates entity, invalid entity, scenario $ref resolution
- No field definitions duplicated in frontend or backend: Python backend reads files directly; frontend fetches via /api/schema/*; C# would load same files
- tsc --noEmit: clean; vitest: 39/39 pass (10 new)
- PRD item confirmed passing: "JSON Schema contract files committed to repo as single source of truth"

2026-04-30: Verified API contract file (contracts/api.md already complete); added 27 programmatic tests.
- contracts/api.md already had all 7 endpoints, all status codes (200/201/204/404/409/422), and full 56-byte binary wire format spec
- Created src/apiContract.test.ts: 27 tests reading api.md via Node fs; verifies file non-empty, all REST endpoints present, all status codes present, 56-byte header, Float32Array, all wire field names (magic/version/width/height/min_val/max_val/west/south/east/north/reserved)
- tsc --noEmit: clean; vitest: 66/66 pass
- PRD item now passing: "API contract file defines all endpoint signatures, request/response shapes, and binary slice wire format"

2026-04-30: SchemaFormRenderer tested in isolation via jsdom + @testing-library/react.
- frontend/src/SchemaFormRenderer.test.tsx already existed with 8 tests covering: string/number/boolean/enum field rendering, x-show-if hide (condition false), x-show-if show (condition true), dynamic toggle via fireEvent.change, arbitrary schema with no hardcoded names
- Uses // @vitest-environment jsdom; real useForm() from react-hook-form (no mocks); fixture schema with string/number/boolean/enum/conditional fields
- tsc --noEmit: clean; vitest: 74/74 pass
- PRD item now passing: "SchemaFormRenderer accepts a schema object and react-hook-form control as props, testable in isolation"

2026-04-30: Implemented form validation on blur and submit (type checks + min/max constraints).
- SchemaFormRenderer.tsx: added numericRules() helper (validate: isNumber/min/max), rules prop on number Controller, ErrorMsg shown via fieldState.error?.message
- errors?: FieldErrors prop added to SchemaFormRendererProps (optional, unused in render — errors come from fieldState)
- SchemaFormRenderer.test.tsx: added ValidationWrapper (mode:'onBlur') + 5 validation tests: blur-empty→"Must be a number", error-clears-on-valid, min constraint error, max constraint error, submit blocked + errors shown
- tsc --noEmit: clean; vitest: 79/79 pass
- PRD item now passing: "Form validation runs on blur and on submit, enforcing type checks and min/max constraints"
